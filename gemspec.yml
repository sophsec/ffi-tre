name: ffi-tre
summary: Ruby FFI bindings for libtre.
description: Ruby FFI bindings for libtre.
license: MIT
authors: Postmodern
email: postmodern.mod3@gmail.com
homepage: http://github.com/sophsec/ffi-tre
has_yard: true

requirements: libre >= 0.8.0

dependencies:
  ffi: ~> 1.0

development_dependencies:
  rubygems-tasks: ~> 0.1
  rspec: ~> 2.0
  yard: ~> 0.7
